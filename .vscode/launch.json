{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug pg_test",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/target/debug/pg_test",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "cargo build",
        }
    ]
}
